function EstatesTable() {
  // 1) FILTER
//   const filterValue = searchParams.get("discount") || "all";

//   let filteredCabins;
//   if (filterValue === "all") filteredCabins = people;
//   if (filterValue === "appartemant")
//     filteredCabins = people.filter((people) => people.type === "آپارتمانی");
//   if (filterValue === "villa")
//     filteredCabins = people.filter((people) => people.type === "ویلایی");

  return <div></div>;
}

export default EstatesTable;
